/** @type {import('tailwindcss').Config} */
export default {
  darkMode: ["class"],
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  // Product appearance is owned by Dream CSS. Tailwind remains available only
  // for structural composition utilities guarded by audit:appearance.
  theme: {},
  plugins: [],
};
