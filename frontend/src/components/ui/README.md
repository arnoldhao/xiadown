# UI Vendor Components

This directory is treated as vendor output from `npx shadcn@latest add`.
Do not edit these files by hand. If you need app-specific changes, add a wrapper
or override in `frontend/src/shared/ui` and import from there.
